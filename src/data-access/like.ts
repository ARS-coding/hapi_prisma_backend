import { prisma } from "../prisma";

export function createLike(post_id: string, user_id: string) {
  return "createLike";
}

export function deleteLike(post_id: string, user_id: string) {
  return "getRecentPosts";
}
